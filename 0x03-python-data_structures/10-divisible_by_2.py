#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    divis = []

    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
            divis.append(true)
        else:
            divis.append(false)
    return (divis)
